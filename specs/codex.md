# Codex Guardrails
- Never commit secrets. Use process.env/*.env.local.
- Follow specs/*.md acceptance criteria strictly.
- Add tests for pricing engine (Jest).
- UI uses shadcn components; no inline styles if 불필요.
- Create small, reviewable PRs with clear commit messages.
